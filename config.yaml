
# 应用程序配置 (版本 33)
application:
  name: "finance-app-v33"

# 数据库连接信息
database:
  type: mysql
  host: "prod-db-server.internal.net"
  user: "admin_user"
  # 注意：这是不安全的！
  password: "${MYSQL_DB}@ssw0rd_33!"
  db_name: "production_db"
