
# 应用程序配置 (版本 67)
application:
  name: "finance-app-v67"

# 数据库连接信息
database:
  type: mysql
  host: "prod-db-server.internal.net"
  user: "admin_user"
  # 注意：这是不安全的！
  password: "${MYSQL_DB}@ssw0rd_67!"
  db_name: "production_db"
